name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/login-action@v3.0.0
        with:
          username: ${{ secrets.SECRET_RACO1 }}
          password: ${{ secrets.SECRET_RACO }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5.0.0
        with:
          context: ./src
          file: ./src/Dockerfile
          push: true
          tags: |
            raco21/kube-news:latest

      - name: Notify Failure
        if: ${{ failure() }}
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          from: ${{ secrets.EMAIL_USERNAME }}
          subject: "Approval Job Failed - ${{ github.repository }}"
          body: "O job de aprovação falhou para o repositório ${{ github.repository }}. Confira o workflow para mais detalhes."
          to: ${{ secrets.DEVELOPER_EMAIL }}

      # Adicionar etapa para criar uma release no GitHub
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v3.3.3   # Substitua por sua versão dinâmica
          release_name: Release v3.3.3
          body: |
            Esta é a versão 3.3.3 da aplicação.
            - Lista de mudanças relevantes no release.
          draft: false
          prerelease: false

      # Adicionar etapa para fazer upload do artefato
      - name: Create ZIP Artifact
        run: zip -r artifact.zip ./build

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./artifact.zip   # Ajuste o caminho do seu artefato
          asset_name: artifact.zip
          asset_content_type: application/zip

  CD:
    runs-on: ubuntu-latest
    needs:
      - CI
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set Kubernetes Context
        uses: Azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.K8S_CONFIG }}

      - name: Deploy to Kubernetes cluster
        uses: Azure/k8s-deploy@v4.9     
        with:
          images: raco21/kube-news:latest
          manifests: |
            k8s/deployment.yaml

      - name: Get Application URL
        run: |
          APP_IP=$(kubectl get svc kubenews -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          echo "Application is available at http://${APP_IP}"

      - name: Notify on CI Approval
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          from: ${{ secrets.EMAIL_USERNAME }}
          subject: "CI Job Approved - ${{ github.repository }}"
          body: |
            O job CI foi aprovado para o repositório ${{ github.repository }}.
            A aplicação está disponível
          to: ${{ secrets.DEVELOPER_EMAIL }}